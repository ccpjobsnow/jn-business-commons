package com.jn.commons.utils;

public enum JnAsyncBusiness {
	executeLogout, updatePassword, executeLogin, sendUserToken, notifyError, 
	notifyContactUs, grouperBalance, grouperSupport, grouperLogin, sendInstantMessage, 
	sendEmailMessage,
	;
}
